name: Test

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go-version: ['1.20', '1.21']
        # Go <=1.23 binaries lack LC_UUID, which dyld on the macOS 15.4+
        # runners intermittently refuses to load; test macOS on stable only
        exclude:
          - os: macos-latest
            go-version: '1.20'
          - os: macos-latest
            go-version: '1.21'
        include:
          - os: macos-latest
            go-version: 'stable'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}
    
    - name: Cache Go modules
      uses: actions/cache@v4
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-
    
    - name: Install dependencies
      run: go mod download
    
    - name: Run tests
      run: go test -v -race -coverprofile coverage.out ./...
    
    - name: Generate coverage report
      run: go tool cover -func coverage.out
    
    - name: Upload coverage to Codecov
      if: matrix.os == 'ubuntu-latest' && matrix.go-version == '1.21'
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.out
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  lint:
    name: Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'
    
    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v6
      with:
        version: latest
        args: --timeout=5m

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        # 1.24+ required: earlier toolchains omit LC_UUID from darwin
        # binaries, which dyld on macOS 15.4+ runners refuses to load
        go-version: ['stable']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}
    
    - name: Build binary
      run: go build -v -o cert${{ matrix.os == 'windows-latest' && '.exe' || '' }} .
    
    - name: Test binary execution
      run: |
        if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
          ./cert.exe --version
        else
          ./cert --version
        fi
      shell: bash
    
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: cert-${{ matrix.os }}
        path: cert${{ matrix.os == 'windows-latest' && '.exe' || '' }}